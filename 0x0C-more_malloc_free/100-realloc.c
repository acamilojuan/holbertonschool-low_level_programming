#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * *_realloc - function
 * *ptr: pointer input
 * @old_size: variable 1 to be used
 * @new_size: variable 2 to be used
 */
void *_realloc(void *ptr, unsigned int old_size, unsigned int new_size)
{
	if (new_size > old_size)
		s = malloc(
	
	if (new_size == old_size)
		return (ptr);

	if (ptr == NULL)
		free(ptr);
		return (NULL)
	
		
}
