#include "holberton.h"
/**
 * factorial - Function
 * @n: input variable to be used
 * Return: factorial
 */
int factorial(int n)
{
	int a = 0;

	if (s[a] != '\0')
	{
	a = factorial((void *)(s + 1));
	a++;
	}
	return (a);
}
