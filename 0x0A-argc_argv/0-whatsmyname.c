#include <stdio.h>
/**
 * main - function
 * @argc: input argument
 * @argv: input string to be used
 * Return: Always zero
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
